# Functional Status

_Last updated: 2026-09-15 (**v0.5.0 shipped**; **M25 MAT loop depth** shipped on `feature/mat-maturity-m25-plus` — evidence [docs/evidence/m25-mat-loop-depth.md](docs/evidence/m25-mat-loop-depth.md); next = M26 async platform; M24.E compare remains gated; native launch / MAT golden still open — [docs/evidence/m21-m22-remaining.md](docs/evidence/m21-m22-remaining.md))_

This document captures where the current alpha build stands versus the roadmap described in `README.md` and `ARCHITECTURE.md`. Use it to see what already works, what is partially there, and which gaps remain before we can call the heap analyzer "functionally complete".

## Snapshot
- ✅ **M25 MAT investigation-loop depth (command/UI evidence)** — Bounded sortable histogram + class→instances→Inspector; session-retained DominatorTree with lazy children; opt-in field inspection with cost disclosure; synchronized refs/referrers/dominators/GC-path nodes. Evidence: [docs/evidence/m25-mat-loop-depth.md](docs/evidence/m25-mat-loop-depth.md). Packaged GUI smoke **NOT proven** on WSL. Next: **M26** async platform on `feature/mat-maturity-m25-plus`.
- 🧭 **UI → MAT maturity program (executing) → M26** — Approach C continues. Plans: [M26](docs/superpowers/plans/2026-09-15-m26-async-platform.md), [M27](docs/superpowers/plans/2026-09-15-m27-durable-investigations-compare.md), [M28](docs/superpowers/plans/2026-09-15-m28-power-tool-completeness.md), [M29](docs/superpowers/plans/2026-09-15-m29-guided-investigation-continuity.md), [M30](docs/superpowers/plans/2026-09-15-m30-product-hardening.md), and [gated M31+](docs/superpowers/plans/2026-09-15-m31-gated-follow-ons.md).
- ✅ **v0.5.0 shipped — M24 Continuous Heap Investigation A–D + M32** — Tag and GitHub release are published. Persistent **Open / Open another / Close**, transactional recent/snapshot open, shared `objectId` / `classKey` / `leakId` selection, and the findings/Assistant advisory flow meet the scoped continuous-shell criteria. **M24.E in-workbench compare remains gated post-v0.5.0**; the existing standalone `/compare` route remains available. See [release notes](docs/release-notes-v0.5.0.md).
- 🟡 **M32 Dependency & toolchain currency** — React 19.3, Vite 8.3, Tailwind 4.3, TypeScript 7, Cargo/Docker/Actions currency, and related pins landed. **jsdom remains on 24** because jsdom 29/30 correlated with multi-GB Bun RSS / hangs in this environment; jsdom 30 is deferred, not silently omitted. Inventory: [docs/product/dependency-currency-inventory.md](docs/product/dependency-currency-inventory.md).
- 🟡 **v0.4.2 (desktop Open-heap hotfix)** — camelCase pick-file fields + `formatHostError` so real host errors surface; DevTools tip on Validation Console. Notes: [docs/release-notes-v0.4.2.md](docs/release-notes-v0.4.2.md).
- 🟡 **v0.4.1 (desktop bridge injection)** — Host bridges from `ui/src/host/tauri-bridge.ts` + ACL. Superseded for Open-heap by **v0.4.2**. Notes: [docs/release-notes-v0.4.1.md](docs/release-notes-v0.4.1.md).
- 🟡 **M23 AI-first guided investigation (slices shipped; final Terra/Sol closeout pending)** — `/assistant` Investigation session (rules default, fact/AI separation + provenance); desktop workflow get/close/resume; thin Tauri create/resume/get/close/`chatSession` adapters with 12/32 bounds and rules fallback. **Does not** claim AI replaces MAT-equivalent analysis. Evidence (synthetic scenario + NOT-proven WSL GUI / live provider): [docs/evidence/m23-guided-investigation.md](docs/evidence/m23-guided-investigation.md). Design: [docs/design/milestone-23-ai-first-guided-investigation.md](docs/design/milestone-23-ai-first-guided-investigation.md). Plan: [docs/superpowers/plans/2026-09-14-ui-first-mat-install-ai-plan.md](docs/superpowers/plans/2026-09-14-ui-first-mat-install-ai-plan.md).
- 🟡 **M20 UI workbench (surfaces shipped; milestone closeout pending Terra/Sol)** — detail panels; desktop **Open heap dump** + opaque `sourceId` + `run_desktop_analysis`; classloader Loaded vs Unique; **Policies**, **Flamegraphs**, **Snapshots** (list/save/remove/**open**). Capability matrix: [docs/product/ui-capability-matrix.md](docs/product/ui-capability-matrix.md). Evidence: [docs/evidence/m20-ui-workbench.md](docs/evidence/m20-ui-workbench.md).
- 🟡 **M21 portable install (partial)** — **21.A–E** command/docs layer: frozen names, ditto macOS zip, AppImage/DMG normalize, unsigned desktop SHA256SUMS + provenance, zip secret inspect, Info.plist/ELF probe, README frozen primaries. **Release CI name/checksum + structural probe now fail-closed** (still ≠ launch proof). Evidence: [docs/evidence/m21-portable-installability.md](docs/evidence/m21-portable-installability.md). Remaining host-blocked: native launch matrix / dry-run / screenshots / Terra·Sol closeout — [docs/evidence/m21-m22-remaining.md](docs/evidence/m21-m22-remaining.md).
- 🟡 **M22 bounded MAT OQL (partial)** — **22.A** corpus first slice; **22.B** multi-class `FROM`; **22.C** 1–3 hop `OBJECTS` (+ Terra hop/cycle fixes); **DISTINCT OBJECTS**; **22.D** CLI Unique Classes column + honest flat superclass hierarchy (expand/collapse only with explicit `parentKey`). **Remaining:** corpus Terra/closeout (22.E).
- ✅ **M18 MCP agent/IDE loop completion** — `ci_check` (+ baseline/`baseline_snapshot`), `diff_heaps.cross_reference_leaks`, `save_snapshot`/`remove_snapshot`, `generate_flamegraph` + managed artifacts; sanitized transcripts in [docs/examples/mcp-agent-loop-transcripts.md](docs/examples/mcp-agent-loop-transcripts.md).
- ✅ **M19 guided analysis continuity** — duplicate-array + plugin_results panels, superclass live regroup (`regroupHistogram`), shared 12/32 chat history, `classloader_leak` workflow + guided landing card.
- 🟡 **M17 desktop guided UX bridge completion — shipped with caveats post-v0.3.0** (M14→desktop command wiring closed; injection site corrected under Track A): Native commands for seven M14 methods plus later workflow/assistant methods live in `tauri/src/commands.rs`; **UI injection** is `ui/src/host/tauri-bridge.ts` (not the deprecated `tauri/src/bridge.ts` stub). **Evidence (17.A–C + partial 17.D):** `cargo test --features test-fixtures` in `tauri/session-ops/` — 24/24 pass. **17.D not complete:** packaged-desktop GUI smoke deferred to **M21**/native hosts. See [docs/design/milestone-17-desktop-guided-ux-bridges.md](docs/design/milestone-17-desktop-guided-ux-bridges.md).
- ✅ **M16 desktop packaging & distribution landed post-v0.3.0** (closes the B7 backlog item as tagged-release Tauri installers): `.github/workflows/release.yml` gains a `build-desktop` matrix job **configured** for Windows/macOS/Linux Tauri builds (`.msi`/`.exe`, `.dmg`/`.app`, `.deb`/`.AppImage`/`.rpm` targets; artifact upload + release attachment on tagged releases). **Signing is conditional-on-secrets** — Windows (`WINDOWS_CERTIFICATE` + password, thumbprint wired to `tauri.conf.json` when present), macOS (full Apple cert + notarization secret set), Linux (unsigned by design). **No signing credentials are configured in CI today → default artifacts are unsigned.** CI logs a loud line per platform when secrets are absent. **Auto-update explicitly skipped for v1** — no Tauri updater plugin; users re-download from Releases. README Desktop app section documents per-platform install steps and SmartScreen/Gatekeeper workarounds; Homebrew Cask **deferred for v1** (GitHub Releases remains the sole desktop channel). `tauri/Cargo.toml` version synced to `0.3.0`. Desktop bundles ship on **post-M16 tags only** — historical `v0.3.0` has no desktop installers. Windows `.msi`/`.exe` launch-tested locally; macOS/Linux **not launch-tested** (no evidenced tagged CI run yet). See [docs/design/milestone-16-desktop-packaging.md](docs/design/milestone-16-desktop-packaging.md) (roadmap M16, now shipped with caveats). M17 subsequently wired all M14 bridge methods into Tauri (see M17 snapshot above).
- ✅ **M15 MAT backend parity completion landed post-v0.3.0** (closes the four remaining pure analysis-capability gaps from the MAT parity matrix): new `core::analysis::array_analysis` module (`analyze_duplicate_arrays()`, `DuplicateArrayGroup`, optional `ArrayReport` on `AnalyzeResponse`) wired via CLI `analyze --duplicate-arrays` and MCP `analyze_heap` `enable_duplicate_arrays: boolean`; `HistogramGroupBy::Superclass` plus CLI `--group-by superclass` and MCP `histogram_group_by: "superclass"`; OQL extensions in `core::query` — `outbounds(...)`/`inbounds(...)`/`dominators(...)` traversal functions (15.C), regex `=~` operator (15.D), one-level subqueries (`FROM OBJECTS (SELECT ...)`) and `UNION` with object-id deduplication (15.E); and new `core::plugin` module (15.F) shipping Phase-2 static registration only (`AnalyzerPlugin`, `ReportFormatterPlugin`, `PluginRegistry`, `analyze_heap_with_plugins`, `render_report_with_plugins`, `OutputFormat::Custom(name)`) — no dynamic `cdylib` loading, no CLI `--plugin` flag (Phase 3 stays gated). Named OQL deferrals remain honest: `eval(...)`, multi-class `FROM`, arbitrary-depth nesting, multi-hop `OBJECTS`. UI surfacing of M15 items deferred per M14 backend-before-UI pattern. See [design/milestone-15-mat-backend-parity.md](docs/design/milestone-15-mat-backend-parity.md) (roadmap M15, now shipped).
- ✅ **M14 UI backend-parity & AI-native redesign landed post-v0.3.0** (the first UI-track milestone in this series — every prior M8/M9/M10/M11/M13 milestone deliberately deferred UI work): new `/compare` route and `ui/src/features/comparison/` ship the M10 object-level-diff comparison basket (picker, match-quality badge, added/removed/retained-changed tables) behind a new dedicated `__MNEMOSYNE_COMPARISON_BRIDGE__`; `ObjectInspectorPanel` renders M8's dominator parent/children and refs as clickable navigation chips via a new optional `inspectObject` bridge method, and `LeakGcPathPage` gains a path-count selector rendering every enumerated GC-root path via a new optional `findAllGcPaths` method (both purely additive — a bridge lacking either method renders byte-identical pre-M14 output); new `ReferrerPanel`/`ClassloaderExplorerPanel` (in Artifact Explorer) and a new `/heap-explorer/threads` route (`ThreadExplorerPanel`) surface M8's referrer report, M13's duplicate-class/ancestor-chain data, and M8's per-frame thread locals, all purely artifact-backed with no new bridge calls; and a new AI-guided landing (`GuidedLanding`, appended below the existing unchanged artifact-drop flow) composes a `TriageSummaryCard` driving M11's `triage_memory_leak` workflow end-to-end, a natural-language input bar that routes OQL-shaped text to the existing query console and everything else to `start_workflow`, three more workflow-kind cards, a `RecentHeapsList` backed by `list_snapshots`, and a persistent `TopNav` reaching every power route — all through a fourth new dedicated `__MNEMOSYNE_WORKFLOW_BRIDGE__`. A closing visual-consistency pass (Slice 14.E) found and fixed one heading-hierarchy inconsistency (two `GuidedLanding` children used their own `<h3>` instead of the `<h4>` depth their siblings used) and confirmed no other drift against `ui/src/app/globals.css`'s existing dark/Inter tokens. `bun run test` stands at 285 passing tests (four batches: 112/85/45/43) and `tsc --noEmit` is clean; the Rust workspace is untouched by this milestone and still stands at 790 passing tests, clippy- and fmt-clean. See [design/milestone-14-ui-parity-ai-native.md](docs/design/milestone-14-ui-parity-ai-native.md) (roadmap M14, now shipped).
- ✅ **M11 MCP workflow suite landed post-v0.3.0**: new top-level `core::workflow` module (sibling of `mcp`/`snapshot`/`diff`/`policy`) ships all four designed `WorkflowKind` variants as small orchestration state machines over existing, already-tested primitives -- no new heap-analysis logic anywhere in this milestone. `TriageMemoryLeak` (`detect` → `investigate_suspect` → `explain` → `propose_fix` → `complete`) composes `detect_leaks`/`find_all_gc_paths`/`analyze_by_referrer`/AI insight generation; `TuneGc` (`root_kind_breakdown` → `thread_local_review` → `top_retainers` → `complete`) is a diagnostic-only GC-root retention review that never touches a live JVM; `TraverseObjectGraph` (`inspect` ⇄ `choose_direction` → `complete`) is the one kind with a real caller-driven branch point, rejecting a `choose_direction` id that wasn't actually offered; `CompareSnapshots` (`resolve_snapshots` → `diff` → `complete`) composes M9's `SnapshotStore` and M10's `run_diff`/`DiffMode::Object`. `WorkflowStore` mirrors `McpSessionStore`/`SnapshotStore`'s atomic-write + `schema_version: u32` persistence shape exactly (its third consumer in this crate). Five new MCP tools ship in `core::mcp::server`: `describe_workflow`, `start_workflow`, `next_step`, `get_workflow`, `close_workflow`, with four structured error codes (`workflow_not_found`, `workflow_corrupt`, `workflow_step_input_mismatch`, `workflow_already_complete`) through the standard `error_details` envelope. New `docs/mcp-workflows.md` carries one real, captured (not hand-written) request/response transcript per workflow kind. One scope-drift vs. the design doc's own speculative text: only the `MNEMOSYNE_WORKFLOW_DIR` env var shipped for the on-disk store root, not the additionally-speculated `[workflow].directory` config key -- the same env-only pattern M9's `SnapshotStore` already established. See [design/milestone-11-mcp-workflow-suite.md](docs/design/milestone-11-mcp-workflow-suite.md) (roadmap M11, now shipped).
- ✅ **M13 classloader explorer & leak detection landed post-v0.3.0**: `core::analysis::classloader` is extended, not replaced — `analyze_classloaders()`'s pre-existing `potential_leaks` (a single-loader "retains a lot, loads almost nothing else" heuristic, M3 Phase 3) and the new `duplicate_classes` (cross-loader "same class name loaded by ≥2 distinct classloaders" signal, MAT's actual "Duplicate Classes" report shape and the classic Tomcat/Jetty/Spring hot-redeploy leak pattern) now ship as two permanent, independent fields on `ClassLoaderReport`, neither replacing the other. New `detect_duplicate_classes()` groups `graph.classes` by normalized class name; a class loaded twice by the *same* loader is correctly excluded. `ClassLoaderInfo` gains `unique_class_count` (derived from the same grouping pass; CLI Unique Classes column shipped in M22.D) and `ancestor_chain: Vec<ObjectId>` (new `resolve_loader_chain()`, bounded walk depth 16, visited-set cycle guard against adversarial/malformed HPROF `parent` cycles). `mnemosyne analyze --classloaders` (no new flag) gains a "Duplicate classes across loaders" section (printed only when non-empty) and an "Ancestors" column on the per-loader table. `core::policy` gains the deep-only `classloader_leak_count` predicate (threshold on `duplicate_classes.len()`); a follow-up fix corrected `ci-check`'s handler, which had hardcoded classloader analysis off so the predicate never received real data outside its own unit tests. New MCP tool `detect_classloader_leaks` (`heap_path` in, `Vec<DuplicateClassGroup>` out) mirrors why `diff_heaps` exists standalone. See [design/milestone-13-classloader-explorer.md](docs/design/milestone-13-classloader-explorer.md) (roadmap M13, now shipped).
- ✅ **M8 reachability & references deep dive landed post-v0.3.0**: `mnemosyne gc-path --all-paths [--by-class <class>] [--max-paths <n>]` enumerates every GC root path up to a shared budget (default 20) with an honest `truncated: bool` flag and new exit codes `8`/`9`; `mnemosyne analyze --by-referrer` (new `core::analysis::referrers` module) ranks objects by incoming-reference count via a `ReferrerReport` on `AnalyzeResponse` and MCP `analyze_heap`'s new `by_referrer` param; new `mnemosyne inspect <heap> --object-id <id> [--retain-field-data] [--format text|json|toon]` subcommand and MCP tool `inspect_object` (new `core::analysis::inspector` module + `core::report::inspect` renderers) return shallow/retained size, dominator parent/children, and refs in/out as structured `ObjectRef { object_id, class_name }`; and `analyze --threads` now resolves `ROOT_JAVA_FRAME`/`ROOT_JNI_LOCAL` GC roots into per-frame `local:`/`jni-local:` lines via new `FrameLocal` entries (`variable_slot` honestly reuses the raw HPROF frame number, not a real bytecode slot index). See [design/milestone-8-reachability-references.md](docs/design/milestone-8-reachability-references.md) (roadmap M8, now shipped).
- ✅ **M9 snapshot persistence landed post-v0.3.0**: new `core::snapshot` module (`SnapshotManifest`, `SnapshotPayload`, `SnapshotStore::{save,load,load_checked,list,remove,find_fresh_for_heap}`; `DominatorTree` now derives `Serialize`/`Deserialize`) caches a parsed `(ObjectGraph, DominatorTree)` pair to disk keyed by heap-file SHA-256 under `dirs::cache_dir()/mnemosyne` (`MNEMOSYNE_SNAPSHOT_DIR` override); `mnemosyne snapshot save|load|list|rm` plus additive `--snapshot <hash-or-path>`/`--refresh` flags on `analyze`, `leaks`, `gc-path`, `inspect`, and `query` skip the binary parse on a cache hit, auto-discovering silently when no flag is passed and failing loudly (new exit codes `10`-`13`) only when an *explicit* `--snapshot`/`snapshot load` key is stale, schema-mismatched, corrupt, or missing; MCP gains `open_snapshot`/`list_snapshots` plus an additive `snapshot` param on `analyze_heap`/`parse_heap`/`find_gc_path`/`inspect_object`/`query_heap` (`parse_heap` in snapshot mode returns a distinctly-shaped partial summary with a `ProvenanceKind::Partial` marker rather than a reconstructed `HeapSummary`, since that needs raw HPROF record data a snapshot doesn't retain). M8's analyzer outputs are deliberately **not** precomputed into the snapshot payload — they stay cheap on-demand computations over the loaded graph. See [design/milestone-9-snapshot-persistence.md](docs/design/milestone-9-snapshot-persistence.md) (roadmap M9, now shipped).
- ✅ **M10 object-level heap diff landed post-v0.3.0**: `mnemosyne diff before.hprof after.hprof --mode object` ships fingerprint-based per-object identity tracking (`class+retained` / `class+dominator` default / `full-fingerprint` strategies), `added` / `removed` / `retained_changed` sections with dominator + reference chains, a `MatchQuality` collision-rate envelope, budget guards (`MAX_OBJECT_DIFF_FINGERPRINTS`, `--object-diff-min-retained`), text/JSON/TOON renderers under `core::report::diff`, and MCP `diff_heaps` `mode: "object"`. Class-mode `diff` output is byte-identical to v0.3.0. See [design/milestone-8-1-object-level-diff.md](docs/design/milestone-8-1-object-level-diff.md) (roadmap M10).
- ✅ **M10-B `ci-check` object-growth predicate + leak-progression cross-reference landed**: `ci-check --baseline <BEFORE_HEAP>` runs a `DiffMode::Object` diff up front and feeds the resulting `ObjectDiffReport` into `core::policy::evaluate()`'s new additive `object_diff: Option<&ObjectDiffReport>` parameter (every pre-existing call site passes `None`, byte-identical evaluation for all ten prior predicates). New class-scoped, two-heap `Predicate::ObjectGrowthThreshold` (`object_growth_threshold`, 12th predicate) scans `retained_changed` (`|after - before|`) and `added` (`after_retained_bytes`, since there's no "before") for entries matching the rule's `class` (or every entry when absent), reporting one aggregate `Violation` citing the worst (largest-delta) offender by id plus a count. A policy with such a rule and no `--baseline` fails loudly with a structured `object_growth_threshold_requires_baseline` error (exit code 2) rather than silently evaluating without growth data. Separately, `mnemosyne diff --mode object --cross-reference-leaks` (opt-in, default off) calls `detect_leaks()` on the after-heap and sets a new additive `ObjectDelta.leak_severity: Option<LeakSeverity>` field via `core::diff::object::annotate_leak_progression()`, rendered as a `[LEAK: <severity>]` suffix on `added`/`retained_changed` text lines. MCP wiring for both flags is deferred (CLI-first, matching M10's own delivery order). See [design/milestone-10-b-object-growth-policy.md](docs/design/milestone-10-b-object-growth-policy.md) (roadmap M10, now shipped).
- ✅ **v0.4.0 shipped on 2026-09-14**: tag `v0.4.0` publishes CLI archives, desktop bundles (unsigned by default), GHCR `0.4.0`/`0.4`/`latest`, and [docs/release-notes-v0.4.0.md](docs/release-notes-v0.4.0.md). Homebrew formula carries Intel + Apple Silicon SHA-256 values. Superseded for desktop Open-heap by **v0.4.1**. Host-blocked leftovers unchanged: native launch matrix, MAT golden, Sol closeouts — [docs/evidence/m21-m22-remaining.md](docs/evidence/m21-m22-remaining.md).
- ✅ **v0.3.0 shipped on 2026-04-26**: tag `v0.3.0` published five CLI platform archives and GHCR `0.3.0`/`0.3`/`latest` (no desktop bundles; pre-M16).
- ✅ **M1.5 real-world hardening landed**: `HEAP_DUMP_SEGMENT` parsing now uses the correct HPROF tag constant (`0x1C`), record-tag naming matches the spec, and the workspace now validates `parse`, `analyze`, `leaks`, and `gc-path` against a real JVM heap dump when the optional `resources/test-fixtures/heap.hprof` fixture is present.
- ✅ **HPROF parser** streams headers/record stats and produces a record-category histogram + heap summary without loading entire dumps into RAM.
- ✅ **Object-graph pipeline** now lives under `core::hprof/`, where the shared object graph, binary HPROF parser, and test fixtures are grouped together for the parsing domain. `ObjectGraph` can retain raw instance `field_data`, selective `byte[]`/`char[]` contents, and parsed `STACK_TRACE` / `STACK_FRAME` records for higher-level analyzers, but that retention is now opt-in through `ParseOptions { retain_field_data: true }` so default `analyze`, `leaks`, and `gc-path` runs stay on the lean path.
- ✅ **M3 foundation tooling landed**: `core::hprof::tags` now centralizes top-level and heap-dump sub-record constants for the parser, binary parser, fixtures, and GC-path traversal, and the repository now includes Criterion bench targets plus `scripts/measure_rss.sh` for throughput/RSS baselining.
- ✅ **M3-P1-B2 core analysis features landed**: `analyze_heap()` now emits grouped histograms, unreachable-object summaries, and MAT-style suspect scoring; `diff_heaps()` now attaches class-level deltas when both dumps build object graphs; and the CLI exposes `analyze --group-by class|package|classloader` plus class-diff table output.
- ✅ **M3 Phase 2 investigation features landed**: `analyze_heap()` can now attach optional thread, string, collection, and top-instances reports; the core ships `inspect_threads()`, `analyze_strings()`, `inspect_collections()`, and `find_top_instances()`; and the CLI exposes `analyze --threads --strings --collections --top-instances` plus tuning flags `--top-n` and `--min-capacity`.
- ✅ **Step 11 large-dump scaling validation landed**: `ParseOptions` now controls field-data retention, `analyze_heap()` only opts into higher-memory parsing when string/collection/thread investigation is requested, `detect_leaks()` and `gc-path` stay lean by default, the enhanced `scripts/measure_rss.sh` profiles `parse`, `analyze`, and `leaks` with ratio markers and `/proc/PID/status` fallback sampling, and the dense synthetic validation batch now clears ~500 MB / ~1 GB / ~2 GB tiers with default-path RSS ratios around 2.87x-2.90x and investigation-path ratios around 3.89x-3.92x.
- ✅ **M7-1 streaming overview mode landed**: `AnalysisMode { auto, deep, overview }` is now shared across core, CLI, MCP, and report rendering. `core::hprof::overview` streams class-resolved top-N aggregates, GC-root counts, and capped thread-frame samples without ever building an `ObjectGraph`, `auto` flips to overview at 4 GiB by default and can be overridden with `MNEMOSYNE_OVERVIEW_AUTO_THRESHOLD`, and overview output is explicitly partial with approximate shallow sizes only, not retained sizes.
- ✅ **M7-2 CI regression policies landed**: `core::policy` now loads dedicated TOML policy files, evaluates 10 predicates (7 overview-compatible plus deep-only `leak_count`, `retained_size`, and `dominator_root_count`) against deep or overview inputs, renders text/JSON/JUnit/GitHub Actions output, and `mnemosyne-cli ci-check` ships the severity-aware exit-code contract (`0` clean, `1` violation, `2` invalid policy, `3` unreadable heap/analyze failure, `4` explicit overview-mode mismatch).
- ✅ **M7-3 allocation-site flame graphs landed**: `core::report::flamegraph` now collapses graph-backed deep analysis into dominator, class-hierarchy, or GC-root-path folded stacks and renders SVG via `inferno`, plus folded-stack text and a structured JSON envelope. `mnemosyne-cli flamegraph` is deep-mode-only, uses the new public `analyze_heap_with_graph()` entry point to access graph internals without widening `AnalyzeResponse`, and exits `5` when the user requests or auto-resolves to overview mode.
- ✅ **M7-4 OQL targeted expansion landed**: `core::query` now ships the approved six-feature MAT-parity slice end-to-end: `@retainedSize` comparisons, synthetic `@toString`, `@gcRootPath` projection/filtering, `LIKE` / `CONTAINS`, `OBJECTS` single-hop projection, and `IS NULL` / `IS NOT NULL`, plus single-quoted string literals. The query layer now reuses a shared `core::graph::gc_root_path` helper, `core::query::synth` owns synthetic string rendering, and deep-only query features map to structured `FeatureUnavailableInOverviewMode` errors when callers reuse the query engine without a deep graph.
- ✅ **CLI surface** (`parse`, `leaks`, `analyze`, `ci-check`, `flamegraph`, `diff`, `map`, `gc-path`, `query`, `explain`, `chat`, `fix`, `serve`, `config`) all call into the shared core. `parse`, `analyze`, `ci-check`, and `flamegraph` now share `--mode auto|deep|overview` resolution where appropriate; `ci-check` adds `--policy`, `--format text|json|junit|github-actions`, `--output`, and `--fail-on info|warning|error|critical`; `flamegraph` adds `--root dominator|class-hierarchy|gc-root-path`, `--format svg|folded-stack|json`, required `--output`, and exit code `5` for overview-mode mismatch; `parse` summary sections and `leaks` output render aligned terminal tables at the CLI boundary; `chat` now ships as a CLI-first leak-focused conversation mode with `/focus <leak-id>`, `/list`, `/help`, and `/exit`; and follow-up disclosure sections still appear when width-bounded cells truncate record-category names, leak IDs, or class names.
- ✅ **Query surface** now covers the targeted M7-4 OQL slice without breaking existing queries: built-in pseudo-attributes `@retainedSize`, `@toString`, and `@gcRootPath`; `LIKE` / `CONTAINS` on string-capable fields; `OBJECTS x.field`; `IS NULL` / `IS NOT NULL`; and single-quoted string literals. The shared CLI error mapping reserves exit code `6` for deep-only query features that are evaluated without a deep graph.
- ✅ **Zero-result UX** is now explicit: `mnemosyne leaks` prints `No leak suspects detected.` when analysis completes without candidates, and `mnemosyne fix` already returns `No fix suggestions available for the provided criteria.` when nothing matches the requested filters.
- ✅ **Error handling** now uses structured `CoreError` variants plus CLI `hint:` lines for missing heap dumps, wrong file types, HPROF header parse failures, and invalid config files.
- ✅ **Leak detection** is now unified: `detect_leaks()` attempts object-graph → dominator → retained-size analysis first, then falls back to heuristics with explicit `ProvenanceKind::Fallback` markers. `explain --leak-id`, `fix --leak-id`, and MCP `explain_leak` now validate the requested leak identifier up front and return `CoreError::InvalidInput` for unknown IDs instead of silently widening to the full leak set.
- ✅ **Graph/dominator view** now supports real retained sizes in both `analyze_heap()` and `detect_leaks()`, with the lightweight summary preview retained only as fallback.
- ✅ **GC path finder** now prefers full `ObjectGraph` BFS first, then falls back to a budget-limited `GcGraph`, then synthetic paths when the heap lacks enough detail.
- ✅ **Object-graph navigation API** now exposes `get_object(id)`, `get_references(id)`, and `get_referrers(id)` for programmatic heap exploration.
- ✅ **M3 completion batch is now landed and verified**: beyond the earlier M3 Phase 3 classloader/query/profile work, the branch now includes optional `hyperfine` / `heaptrack` benchmark wrappers with graceful skip behavior plus the deeper live query slice with retained instance-field projection/filtering on CLI/MCP query paths and hierarchy-aware `INSTANCEOF`.
- ✅ **M6 ecosystem, community, and UI completion landed**: the shared React frontend under `ui/` now spans the local artifact loader, triage dashboard, artifact explorer, heap explorer, and leak workspace. Heap explorer panes now resolve selected objects back to leak IDs for direct cross-navigation into the leak workspace, and the Object Inspector can load live references/referrers when a host bridge is present. The repository also now ships the new user guide, troubleshooting guide, benchmark comparison doc, CI integration guides, example projects, and expanded contributor/community guidance.
- 🟡 **Tauri desktop app (`build-desktop` configured; unsigned default; bridges owned by UI host)**: `tauri/` bundles the shared `ui/` build; `ui/src/host/tauri-bridge.ts` injects all host bridges at bootstrap (desktop heap, heap explorer, leak workspace, comparison, workflow, assistant). ACL permission `allow-desktop-commands` grants the main window invoke access. Deprecated stub: `tauri/src/bridge.ts` (never entered `ui/dist`). The M16 `build-desktop` job is configured in `release.yml` for post-M16 tags; default unsigned unless release signing secrets are configured; no in-app auto-updater in v1; Windows launch-tested locally under M16, macOS/Linux not launch-tested; packaged GUI smoke on WSL blocked by WebKitGTK/GTK deps.
- ⚠️ **AI insights** now support three explicit modes: `rules` (default), `stub`, and `provider`. Provider mode now supports YAML-backed prompt templates through an embedded default plus optional `[ai.prompts].template_dir` overrides, plus Step `14(d)` privacy controls under `[ai.privacy]` for `heap_path` redaction, regex-based prompt redaction, opt-in hashed audit logging before external model calls, and a minimal prompt-budget guard. Step `14(e)` now ships both a CLI-first conversation slice through `mnemosyne-cli chat <heap.hprof>` with bounded in-process history and top-3 shortlist context, and persisted MCP AI sessions for resumed chat/explain/fix follow-up. `fix` / `propose_fix` now attempt an AI-backed source-aware patch path when provider mode plus mapped source context are available. MCP transport hardening now has direct `serve` coverage for delayed AI-backed responses and larger payloads, and provider failures/timeouts surface dedicated machine-readable MCP error codes. Streaming remains unnecessary on the verified branch contract.
- ✅ **Report/export** supports Text/Markdown/HTML/TOON/JSON with `--output-file`, plus `mnemosyne flamegraph` SVG/folded-stack/JSON exports through `core::report::flamegraph`. HTML output is XSS-hardened; TOON values are properly escaped; flamegraph SVG is rendered via `inferno`; provenance markers are rendered in all non-JSON analyze formats; and overview-mode renderers now lead with explicit streaming-triage banners plus honest approximate-shallow-size labeling.
- ✅ **Provenance system** labels synthetic, partial, fallback, and placeholder data across analysis responses, leak insights, GC paths, and fix suggestions. CLI and report renderers surface these markers to consumers.
- ✅ **Output hardening** — HTML escaping prevents XSS in report output; TOON escaping handles control characters and backslashes correctly.
- ✅ **Development workflow** now includes GitHub Actions CI, the `test-fixtures` cargo feature for canonical fixture reuse, real-world HPROF validation that skips gracefully when the large fixture is absent, focused wrapper coverage for `scripts/tests/test_run_hyperfine_bench.sh` and `scripts/tests/test_run_heaptrack_profile.sh`, and current full-worktree verification with 448 passing Rust tests (`cargo test`; doctests 0).
- ✅ **Release automation** now validates `v*` tags against the workspace version, cross-compiles `mnemosyne-cli` for five targets, packages archives, publishes GitHub Releases with attached binaries, and builds/pushes the GHCR Docker image on tagged releases.
- ✅ **Packaging metadata + Homebrew scaffolding** now cover crates.io-ready workspace metadata across `Cargo.toml`, `core/Cargo.toml`, and `cli/Cargo.toml`, plus a macOS Homebrew formula for both Apple Silicon and Intel release archives.

## Capability Checklist
| Area | Status | Notes | Next Step |
| --- | --- | --- | --- |
| Parser streaming + histogram | ✅ | `core::hprof::parser` still parses headers + record stats for fast record-category summaries, and CLI parse tables label aggregate bytes/share/entries accurately while disclosing full category names when truncation occurs. | Keep summary parsing aligned with the deep and overview paths. |
| Streaming overview mode | ✅ | `core::hprof::overview` now provides class-resolved, bounded-memory triage without building the `ObjectGraph`; CLI and MCP expose `auto|deep|overview`, `auto` defaults to overview at or above 4 GiB unless `MNEMOSYNE_OVERVIEW_AUTO_THRESHOLD` overrides it, and overview results are explicitly partial with approximate shallow sizes only. | Complete the native-Linux reference-spec rerun for M7-5; the published partial WSL run covers overview vs `hprof-slurp` on `small`/`medium`/`large` only. |
| CI regression policies | ✅ | `core::policy` is now the dedicated post-analysis policy layer: it loads TOML rules, consumes finalized deep or overview results through `PolicyInput`, emits `PolicyResult`, and renders `text`, `json`, `junit`, and `github-actions` outputs for `mnemosyne-cli ci-check`. `--fail-on` uses the `info < warning < error < critical` ladder for exit-code gating, while the reports still show every violation and skipped rule. | Keep the policy surface stable while M7-5 lands adjacent workflows. |
| Flame graphs | ✅ | `core::report::flamegraph` now projects deep-mode graph analysis into dominator, class-hierarchy, and GC-root-path folded stacks and renders SVG, folded-stack text, or a JSON envelope through `mnemosyne-cli flamegraph`. The command requires deep mode, uses `analyze_heap_with_graph()` to keep graph internals out of the serialized `AnalyzeResponse`, and exits `5` when overview mode is requested or auto-resolved. | Reuse the folded-stack/JSON artifacts in future MCP or UI surfaces only if M8 evidence justifies it. |
| OQL targeted + M15 bounded expansion | ✅ | `core::query` ships M7-4 (`@retainedSize`, synthetic `@toString`, `@gcRootPath`, `LIKE`, `CONTAINS`, `OBJECTS`, `IS NULL`/`IS NOT NULL`, single-quoted literals) plus M15 (`outbounds`/`inbounds`/`dominators`, regex `=~`, one-level subqueries, `UNION`). Named deferrals: `eval(...)`, multi-class `FROM`, arbitrary-depth nesting, multi-hop `OBJECTS`. | Extend only via explicit design-doc scope. |
| Object-graph foundation | ✅ | `core::hprof::object_graph` defines the canonical model and `core::hprof::binary_parser` now populates it from binary HPROF records, selective field/array retention, and stack trace / frame metadata. `ParseOptions` makes raw `field_data` retention opt-in so default graph-backed commands stay lean while investigation analyzers can still request full field extraction. | Reuse the same graph across more analysis surfaces. |
| Leak detection | ✅ | `detect_leaks()` now shares the graph-backed object-graph → dominator → retained-size path used by `analyze_heap()`, with heuristic fallback labeled via provenance markers. `validate_leak_id()` now guards `explain`, `fix`, and MCP `explain_leak` so unknown leak IDs fail fast with `CoreError::InvalidInput`. | Extend retained-size-backed suspect ranking into diffing and richer comparison flows. |
| Dominators / retained size | ✅ | `core::graph::dominator` computes real retained sizes, and both `analysis::analyze_heap()` and `detect_leaks()` consume them when parsing succeeds; summary preview remains as fallback only. | Reuse dominator-backed data in diffing and future explorer surfaces. |
| MAT-style suspect ranking | ✅ | Graph-backed leak ranking now computes `LeakSuspect` scores from retained/shallow ratio, accumulation-point detection, dominated counts, and short reference chains, while keeping `LeakInsight` backward compatible via optional fields. | Reuse suspect scoring across more explorer and MCP surfaces. |
| Histogram grouping | ✅ | `build_histogram()` groups graph-backed analysis by class, package, classloader, or superclass (`HistogramGroupBy::Superclass`, M15 Slice 15.B), and CLI `analyze --group-by` renders top groups with instance, shallow-size, and retained-size totals. MCP `histogram_group_by` accepts `"superclass"`. | Extend grouped histogram data into future MCP and interactive explorer views. |
| Unreachable objects | ✅ | `find_unreachable_objects()` walks from GC roots and reports total unreachable count/shallow size plus per-class breakdown when graph-backed analysis succeeds. | Add dedicated views and richer drill-down in future report/UI work. |
| Thread inspection | ✅ | `inspect_threads()` parses thread objects, correlates `ROOT_THREAD_OBJECT` roots with stored `STACK_TRACE` / `STACK_FRAME` records, and reports per-thread retained bytes, thread-local counts, and stack frames. `inspect_threads()` now also cross-references `ROOT_JAVA_FRAME` / `ROOT_JNI_LOCAL` GC roots into per-frame `FrameLocal { variable_slot, object_id, class_name, root_kind }` entries, printed as `local:`/`jni-local:` lines under each frame in `analyze --threads`. | Extend the same data into MCP/API and future explorer views. |
| Group by referrer | ✅ | New `core::analysis::referrers` module (`analyze_by_referrer()`) ranks objects by incoming-reference count (tiebreak retained size) with up to 5 top referrer classes per entry. CLI `analyze --by-referrer` and MCP `analyze_heap` `by_referrer: boolean` param populate an optional `ReferrerReport` on `AnalyzeResponse`. Browser UI: new `ReferrerPanel` in Artifact Explorer (M14 Slice 14.C). | None planned; reused by the M11 workflow suite and the M14 UI. |
| Object inspector (CLI/MCP) | ✅ | New `core::analysis::inspector` module (`inspect_object()`) and `core::report::inspect` renderer family back `mnemosyne inspect <heap> --object-id <id> [--retain-field-data] [--format text\|json\|toon]` and MCP tool `inspect_object`. Refs and dominator context ship as structured `ObjectRef { object_id, class_name }`, not baked strings. Unknown object id exits `8`. Browser UI: `ObjectInspectorPanel` now renders refs/dominator context as clickable navigation chips via a new optional `inspectObject` bridge method (M14 Slice 14.B). | None planned; both the CLI/MCP gap and the browser UI gap are now closed. |
| String analysis | ✅ | `analyze_strings()` decodes retained `java.lang.String` backing arrays, reports duplicate groups and dedup waste, and ranks the largest strings by logical byte length. `char[]` backing arrays now report logical character count instead of raw UTF-16 byte count. | Add richer drill-down and future exporter support. |
| Duplicate primitive-array detection | ✅ | New `core::analysis::array_analysis` (`analyze_duplicate_arrays()`, `DuplicateArrayGroup`, `ArrayReport`) mirrors string duplicate detection for primitive arrays. CLI `analyze --duplicate-arrays` and MCP `analyze_heap` `enable_duplicate_arrays: boolean` attach an optional `array_report` on `AnalyzeResponse`. Requires field-data retention. Shipped M15 Slice 15.A. | None planned. |
| Plugin/extension runtime (Phase 2) | ✅ | New `core::plugin` module ships `AnalyzerPlugin`, `ReportFormatterPlugin`, and `PluginRegistry` with compile-time static registration only. `analyze_heap_with_plugins()` appends to `AnalyzeResponse::plugin_results`; `render_report_with_plugins()` dispatches `OutputFormat::Custom(name)`. Phase 3 dynamic loading remains deferred. Shipped M15 Slice 15.F. | Phase 3 gated on Phase-2 adoption + out-of-tree plugin demand. |
| Collection inspection | ✅ | `inspect_collections()` recognizes `HashMap`, `HashSet`, `ArrayList`, and `ConcurrentHashMap`, computes capacity / fill ratio, and reports oversized or empty collections plus waste totals. Oversized collections now use the inclusive `<= 0.25` fill-ratio threshold instead of a strict `< 0.25` cutoff. | Expand type coverage and reuse the summary in MCP/UI surfaces. |
| Top-N largest instances | ✅ | `find_top_instances()` ranks objects by retained size when dominators are available and falls back to shallow size when they are not. CLI `analyze --top-instances` exposes the report with configurable `--top-n`. | Add per-class drill-down and object-inspection follow-through in future work. |
| Heap diff | ✅ | `diff_heaps()` returns the existing record-level diff, optional `class_diff` deltas, and now optional `object_diff` (`--mode object`) with fingerprint-based per-object identity, `added`/`removed`/`retained_changed` sections, and `MatchQuality`. MCP `diff_heaps` exposes the same `mode` parameter plus additive `cross_reference_leaks` (M18.C). `ci-check --baseline` + `object_growth_threshold` predicate and `diff --mode object --cross-reference-leaks` leak-progression annotation shipped in M10-B; MCP `ci_check` now mirrors baseline (M18.A/B). Browser UI: new `/compare` comparison-basket route loads a precomputed report or runs a live diff (M14 Slice 14.A). | M18.F transcripts still open. |
| Snapshot persistence / parse-once-query-many | ✅ | New `core::snapshot` module (`SnapshotStore::{save,load,load_checked,list,remove,find_fresh_for_heap}`) caches a parsed `(ObjectGraph, DominatorTree)` pair keyed by heap SHA-256 under `dirs::cache_dir()/mnemosyne` (`MNEMOSYNE_SNAPSHOT_DIR` override). `mnemosyne snapshot save\|load\|list\|rm` + additive `--snapshot`/`--refresh` on `analyze`/`leaks`/`gc-path`/`inspect`/`query`; auto-discovery is silent, explicit `--snapshot <key>` fails loudly with exit codes `10`-`13` on a stale/mismatched/corrupt/missing key. Browser UI: `RecentHeapsList` on the guided landing lists cached snapshots via `list_snapshots` (M14 Slice 14.D). | Criterion benchmark comparing cold-parse vs. snapshot-load wall-clock (`core/benches/snapshot_load.rs`) was scoped for Slice 9.D but not shipped; the performance win is unmeasured, not unverified-correct. |
| Snapshot MCP surface | ✅ | New MCP tools `open_snapshot` (key -> manifest) and `list_snapshots`; additive `snapshot: string` param on `analyze_heap`/`parse_heap`/`find_gc_path`/`inspect_object`/`query_heap`. `parse_heap`'s snapshot response is a distinctly-shaped partial object (manifest fields + honestly-computed `total_shallow_size_bytes`, `ProvenanceKind::Partial`) rather than a reconstructed `HeapSummary`, since that needs a raw HPROF record-tag scan a cached snapshot doesn't retain. | `compare_snapshots` (M11) now reuses this surface for cheap re-open. |
| GC root path | ✅ | `core::graph::gc_path` attempts full `ObjectGraph` BFS first, then falls back to a budget-limited `GcGraph`, then synthetic paths when necessary. `gc-path --all-paths [--by-class <class>] [--max-paths <n>, default 20]` enumerates every path up to a shared budget with an honest `truncated: bool` flag (new optional `all_paths` field; the existing `path` field is untouched, so default `gc-path` output stays byte-identical). New CLI exit codes `8` (object id not found) / `9` (`--by-class` matches zero live instances). MCP `find_gc_path` gains matching `all_paths`/`by_class`/`max_paths` params. | Keep edge labeling and fallback behavior aligned as more traversal surfaces land. |
| MCP workflow suite (`core::workflow`) | ✅ | New top-level `core::workflow` module ships all four `WorkflowKind` variants as orchestration state machines over already-tested primitives: `TriageMemoryLeak`, `TuneGc` (diagnostic-only, never touches a live JVM), `TraverseObjectGraph` (the one kind with a real caller-driven branch point at `choose_direction`), and `CompareSnapshots` (composes `SnapshotStore` + M10's object diff). `WorkflowStore` mirrors `McpSessionStore`/`SnapshotStore`'s atomic-write + `schema_version` persistence shape. Zero new heap-analysis logic — every step calls an existing, already-tested function. | None planned; a fifth classloader-leak workflow kind remains open future work now that M13 has shipped. |
| MCP workflow lifecycle tools | ✅ | Five new MCP tools in `core::mcp::server`: `describe_workflow`, `start_workflow`, `next_step`, `get_workflow`, `close_workflow`, mirroring the existing AI-session lifecycle tool shape. Four structured error codes (`workflow_not_found`, `workflow_corrupt`, `workflow_step_input_mismatch`, `workflow_already_complete`) through the standard `error_details` envelope. `MNEMOSYNE_WORKFLOW_DIR` overrides the on-disk store root (no `[workflow].directory` config key shipped, matching `MNEMOSYNE_SNAPSHOT_DIR`'s own precedent). New `docs/mcp-workflows.md` carries one real captured transcript per workflow kind. | Workflow state is not automatically evicted; manual `close_workflow` only, same as M9's own snapshot-cache-growth stance. |
| Classloader duplicate-class detection | ✅ | New `detect_duplicate_classes()` in `core::analysis::classloader` groups `graph.classes` by normalized class name and keeps only names declared by ≥2 distinct `class_loader_id` values (MAT's "Duplicate Classes" report shape; a class loaded twice by the *same* loader is excluded). `ClassLoaderReport.duplicate_classes` ships alongside the pre-existing `potential_leaks` field — both signals are permanent, neither replaces the other. `ClassLoaderInfo.unique_class_count` is derived from the same grouping pass. CLI `analyze --classloaders` (no new flag) prints a "Duplicate classes across loaders" section when non-empty and renders Unique Classes from the already-computed `unique_class_count` (M22.D). Browser UI: new `ClassloaderExplorerPanel` in Artifact Explorer renders the same data (M14 Slice 14.C). | None for presentation; MAT-like collapsible superclass tree remains open (flat regroup unless explicit `parentKey`). |
| Classloader ancestor chain + `ci-check`/MCP surface | ✅ | New `resolve_loader_chain()` walks a loader's `parent` field repeatedly (bounded depth 16, visited-set cycle guard) into `ClassLoaderInfo.ancestor_chain`, rendered as an "Ancestors" column in the per-loader CLI table. `core::policy` gains the deep-only `classloader_leak_count` predicate (threshold on `duplicate_classes.len()`, 11th predicate) — a follow-up fix corrected `ci-check`'s handler, which had hardcoded classloader analysis off so the predicate never received real data outside its own unit tests. New MCP tool `detect_classloader_leaks` (`heap_path` in, `Vec<DuplicateClassGroup>` out). | None planned; the browser panel shipped in M14 Slice 14.C (Artifact Explorer, not the leak workspace specifically). |
| AI/LLM integration | ⚠️ | `generate_ai_insights` now supports `rules`, `stub`, and provider-backed execution. `AiMode::Provider` renders provider instructions from a YAML template (embedded default plus optional override directory), redacts `heap_path` plus regex-matched content from the fully rendered outbound prompt via `[ai.privacy]`, optionally emits hashed audit metadata for that redacted prompt, trims leak context when `max_tokens` is configured very low while preserving the instruction section, and parses a strict TOON response back into `AiInsights`; `rules` remains the default offline-safe path. `mnemosyne-cli chat <heap.hprof>` ships the CLI leak-chat slice, MCP now persists heap-bound AI sessions for resumed `chat_session` / `explain_leak` / `propose_fix` follow-up, and `[ai.sessions].directory` can override the default per-user local session store. `fix` / `propose_fix` add an AI-backed one-file / one-snippet patch-generation slice with heuristic fallback and explicit provenance. | Keep provider hardening and only add streaming if verification shows it is needed. |
| Provenance | ✅ | `ProvenanceKind` + `ProvenanceMarker` on `AnalyzeResponse`, `LeakInsight`, `GcPathResult`, `FixResponse`. Rendered in Text/Markdown/HTML/TOON and surfaced in CLI `leaks`/`gc-path`/`fix` output. | No immediate next step; provenance coverage expands as new response surfaces land. |
| Output hardening | ✅ | `escape_html` prevents XSS in HTML reports; `escape_toon_value` handles control chars in TOON. Clippy range-pattern and iterator warnings resolved. | Maintain as new renderers are added. |
| Reporting / exports | ✅ | Text/Markdown/HTML/TOON/JSON analyze reports remain available with provenance markers and `--output-file` support, and the separate `flamegraph` surface adds SVG/folded-stack/JSON exports for retained-size visualization. | Add richer diff visualizations / GUI output (still future). |
| Test fixtures / CI | ✅ | `core::test_fixtures` now ships behind `feature = "test-fixtures"` for integration-test reuse, `build_graph_fixture()` and `build_segment_fixture()` cover canonical heap shapes plus `HEAP_DUMP_SEGMENT` parsing, focused wrapper tests pass for `scripts/tests/test_run_hyperfine_bench.sh` and `scripts/tests/test_run_heaptrack_profile.sh`, and the current worktree verification passed `cargo check`, `cargo test`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo fmt --all -- --check` with 448 passing Rust tests. | Add more real-world heap fixtures and benchmark coverage in future milestones only where useful. |
| Benchmarking / memory profiling | ✅ | Criterion benchmark baselines, Step 11 RSS re-baselines, dense multi-tier validation, and optional `hyperfine` / `heaptrack` wrapper automation with graceful skip behavior are now in place. `scripts/measure_rss.sh` profiles `parse`, `analyze`, and `leaks`, computes RSS:dump ratios, and falls back to `/proc/PID/status` VmHWM sampling when `/usr/bin/time` is unavailable. Current measured tiers are 0.02x/2.90x/2.90x/3.92x at ~500 MB, 0.01x/2.87x/2.87x/3.89x at ~1 GB, and 0.00x/2.89x/2.89x/3.92x at ~2 GB. The repo now also ships the `scripts/bench/` comparative harness plus the published partial WSL report in `docs/benchmarks/comparative-v0.3.0.md`; that publication covers `mnemo-overview` and `hprof-slurp` on `small`/`medium`/`large` only, and it intentionally omits an RSS comparison table because the run used WSL compatibility shims rather than GNU `/usr/bin/time -v`. | Complete the native-Linux reference-spec rerun with MAT, `mnemo-deep`, equivalence, and the `10 GiB` fixture as post-v0.3.0 follow-up evidence. |
| Release automation + packaging | ✅ | `.github/workflows/release.yml` validates `v*` tags against `[workspace.package].version`, builds `mnemosyne-cli` for Linux/macOS/Windows across five targets, packages tar.gz/zip archives, creates a GitHub Release with generated notes plus attached binaries, and builds/pushes `ghcr.io/<owner>/mnemosyne` on tagged releases with `<version>`, `<major>.<minor>`, and `latest` tags. Workspace/core/cli manifests now carry crates.io metadata, `cli` pins a versioned `mnemosyne-core` dependency for publish/install compatibility, and `HomebrewFormula/mnemosyne.rb` adds macOS install scaffolding for Intel + Apple Silicon releases. | ✅ v0.3.0 shipped on 2026-04-26 across GitHub Releases, GHCR, and Homebrew. Maintain formula checksums on future releases. |
| MCP server | ⚠️ | Command handlers share the graph-backed core stack. Beyond earlier tools: M18 adds `ci_check`, snapshot `save`/`remove`, `generate_flamegraph` + managed `read_artifact`/`delete_artifact`, and `diff_heaps.cross_reference_leaks`. Still: `list_tools`, AI session lifecycle, `error_details`, mode-aware parse/analyze. AI via `rules`/`stub`/`provider` with redaction. | M18.F transcripts; streaming only if transport needs it. |
| Browser-first UI | ✅ | `ui/` now ships the local artifact loader, triage dashboard, artifact explorer, heap explorer, leak workspace, comparison, and workflow-landing route families. Heap explorer panes resolve selected objects back to leak IDs for direct cross-navigation, and the Object Inspector can load live references/referrers/dominator context when a host bridge exposes `getReferences`/`getReferrers`/`inspectObject`. Live-detail routes still surface honest unavailable states when `projectRoot`, `objectId`, or a host bridge is absent. `bun run test` stands at 285 passing tests (four batches: 112/85/45/43), `tsc --noEmit` clean. | Keep widening explorer depth only where product evidence justifies it. |
| Comparison basket UI (M14) | ✅ | New `/compare` route and `ui/src/features/comparison/` (`ComparisonPicker`, `MatchQualityBadge`, `ObjectDeltaTable`, `comparison-store.ts`) surface M10's object-level diff: load a precomputed `diff --mode object --format json` report, or run one live via a new dedicated `__MNEMOSYNE_COMPARISON_BRIDGE__.diffObjects()`. Shipped M14 Slice 14.A. | None planned. |
| AI-guided workflow landing (M14) | ✅ | New `ui/src/features/workflow-landing/` composes a `TriageSummaryCard` (drives M11's `triage_memory_leak` end-to-end), a `NaturalLanguageInputBar` (routes OQL-shaped input to the existing query console, everything else to `start_workflow`), `WorkflowCards` (`tune_gc`/`traverse_object_graph`/`compare_snapshots`), and a `RecentHeapsList` (`list_snapshots`, renders nothing when unavailable) — all through a new dedicated `__MNEMOSYNE_WORKFLOW_BRIDGE__`. Appended below the existing, unchanged artifact-drop flow rather than replacing it. A new persistent `TopNav` (rendered on `/`) keeps every power route one click away. Shipped M14 Slice 14.D. | None planned. |
| Desktop shell (Tauri) | 🟡 | `tauri/` wraps the shared React UI, injects all four M14 host bridges, and exposes native commands for heap loading, query, references/referrers, leak explanation, GC-path (single + multi-path), object inspection, object-level diff, M11 workflow orchestration, snapshot listing, source mapping, and fix suggestions. M17 wired all seven required M14 bridge methods (`inspectObject`, `findAllGcPaths`, `diffObjects`, `describeWorkflow`, `startWorkflow`, `nextStep`, `listSnapshots`); evidenced by 24/24 `tauri/session-ops/` tests. M16 `build-desktop` job configured in `release.yml`; tagged CI artifact upload not yet evidenced; **unsigned by default**; no in-app auto-updater in v1; Windows launch-tested locally, macOS/Linux not launch-tested; packaged GUI smoke on WSL blocked. | Configure release CI signing secrets (optional); per-platform launch evidence (M21). |
| Documentation | ✅ | README/ARCHITECTURE/QUICKSTART/STATUS now align with the shipped pipeline, and the repo also includes `docs/user-guide.md`, `docs/troubleshooting.md`, `docs/benchmarks.md`, `docs/integrations/`, and `examples/` walkthroughs. | Keep docs in sync as runtime behavior changes. |

## Remaining Must-Haves Before "Functionally Complete"
Graph-backed leak detection unification, M3 investigation/query follow-through for the approved scope, regression-quality integration coverage, large-dump scaling validation, and the approved M4-M6 UI/ecosystem scope are now complete. The remaining follow-on work is narrower and evidence-driven:

1. **Richer exporters/visualizations beyond the shipped flamegraph surface** (e.g., protobuf or richer diff views) layered atop the existing JSON writer and `--output-file` plumbing.
2. **Configure release CI signing secrets** (optional) to enable signed Windows Authenticode and macOS notarization on future tagged releases; publish per-platform packaged-desktop launch evidence (M21).

Roadmap note: M3, M4, M5, and M6 are complete for their approved milestone scopes. M7 now stands at 6/6 slices closed for `v0.3.0`: M7-1 streaming overview mode, M7-2 CI regression policies, M7-3 allocation-site flame graphs, and M7-4 OQL targeted expansion are complete and verified; M7-5 is 🟡 partial (the shipped harness plus published WSL run cover `mnemo-overview` and `hprof-slurp` on `small`/`medium`/`large`, while the native-Linux reference-spec rerun with MAT, `mnemo-deep`, equivalence, and the `10 GiB` fixture remains pending); and M7-6 is ✅ shipped on 2026-04-26.

## Newly Completed
- **M7-4 OQL targeted expansion** — Added the targeted M7-4 parity slice to the real query engine: `@retainedSize`, `@toString`, and `@gcRootPath`; `LIKE` / `CONTAINS`; `OBJECTS` single-hop projection; `IS NULL` / `IS NOT NULL`; and single-quoted string literals. The batch also lifted shared shortest-GC-root-path helpers into `core::graph::gc_root_path`, added `core::query::synth` for synthetic string rendering, and mapped deep-only query-mode mismatches through structured core/MCP/CLI error surfaces. The batch passed `cargo check`, `cargo test`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo fmt --all -- --check` with 448 passing Rust tests.
- **M7-3 allocation-site flame graphs** — Added the new `core::report::flamegraph` subtree plus the `mnemosyne-cli flamegraph <heap.hprof> -o <file>` command. The shipped surface supports three rooting strategies (`dominator`, `class-hierarchy`, `gc-root-path`), three output formats (`svg`, `folded-stack`, `json`), the deep-mode-only exit-code contract `5` for explicit or auto-resolved overview mismatches, and the public `analyze_heap_with_graph()` entry point so flamegraph callers can reuse the deep analysis pass without changing `AnalyzeResponse`. The batch passed `cargo check`, `cargo test`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo fmt --all -- --check` with 377 passing Rust tests.
- **M7-2 CI regression policies** — Added the top-level `core::policy` subsystem plus the `mnemosyne-cli ci-check <heap.hprof> --policy <file>` command. The shipped policy surface loads dedicated TOML files, evaluates 10 predicates against deep or overview inputs, uses the `info < warning < error < critical` ladder for `--fail-on`, renders text/JSON/JUnit/GitHub Actions outputs, and documents the exit-code contract `0/1/2/3/4` for clean, violation, invalid policy, unreadable heap/analyze failure, and explicit overview-mode mismatch. The batch passed `cargo check`, `cargo test`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo fmt --all -- --check` with 330 passing Rust tests.
- **M7-1 streaming overview mode** — Added `AnalysisMode { auto, deep, overview }`, the bounded-memory `core::hprof::overview` parser, CLI `parse/analyze --mode`, MCP `parse_heap` / `analyze_heap` `mode` parameters, overview renderers with honest approximate-shallow labeling, and the env override `MNEMOSYNE_OVERVIEW_AUTO_THRESHOLD` for auto-mode threshold benchmarking. The batch preserved byte-identical deep-mode JSON defaults and passed `cargo check`, `cargo test`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo fmt --all -- --check` with 268 passing Rust tests.
- **M6 ecosystem, community, and UI completion** — Heap explorer panes now resolve selected objects back to leak IDs so dominator, object-inspector, and query-console routes can jump directly into the leak workspace. The Object Inspector can load live references/referrers when a host bridge is present. The repo now also ships the user guide, troubleshooting guide, benchmark comparison doc, CI integration guides, Java example projects, the in-repo Tauri desktop scaffold, the plugin/extension system design doc, and expanded contributor/community guidance.
- **External-provider AI execution (first slice)** — Added `AiMode::Provider`, provider runtime config (`endpoint`, `api_key_env`, `max_tokens`, `timeout_secs`), a small OpenAI-compatible transport in `core::llm`, strict TOON parsing back into `AiInsights`, and async call-site isolation via `spawn_blocking` so provider mode works under the CLI/MCP Tokio runtime without panicking. `AiProvider::OpenAi` and `AiProvider::Local` now execute real external calls; unsupported providers and malformed responses fail honestly instead of silently pretending AI succeeded. Added unit coverage for missing-API-key and provider-response parsing plus a CLI integration test for `analyze --ai --format json` through provider mode.
- **Configurable AI task runner (rule-engine slice)** — Extended `AiConfig` with `mode` plus ordered task definitions, taught config loading to parse `mode` and `[[ai.tasks]]` / `[[llm.tasks]]`, and rewired `generate_ai_insights()` to execute a configurable rule-based task runner while preserving the existing `AiInsights` / TOON wire contract used by CLI, MCP, and reports. Added unit coverage for config parsing, task disabling behavior, and a CLI JSON contract test for `analyze --ai`.
- **M3 Phase 3 classloader + OQL + profiles** — Added `analysis::classloader` with optional `classloader_report` integration in `analyze_heap()`, shared report-renderer support for classloader sections in text/markdown/html/TOON, CLI `analyze --classloaders`, MCP `analyze_heap`, a minimal OQL parser/executor under `core::query` for built-in fields, CLI `query`, MCP `query_heap`, and analyze profiles `overview`, `incident-response`, and `ci-regression`. Added parser/executor/core/MCP/CLI tests and kept the full workspace green.

## Recently Completed
- **M3 final closeout batch** — Added optional `hyperfine` / `heaptrack` benchmark wrappers with graceful skip behavior, deeper retained instance-field projection/filtering on the CLI/MCP query paths, and hierarchy-aware `INSTANCEOF`. Focused wrapper tests passed via `bash scripts/tests/test_run_hyperfine_bench.sh` and `bash scripts/tests/test_run_heaptrack_profile.sh`, and the current worktree also passed `cargo check`, `cargo test`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo fmt --all -- --check` with 228 passing Rust tests.
- **CLI-first AI conversation mode (Step 14(e) first slice)** — Added `mnemosyne-cli chat <heap.hprof>` as a leak-focused interactive session that analyzes once, shows the top 3 leak candidates, supports `/focus <leak-id>`, `/list`, `/help`, and `/exit`, and keeps only the running process' recent history in memory. Chat reuses the shared `rules` / `stub` / `provider` AI pipeline, respects normal `[analysis]` filters at startup, stays available in healthy-heap mode when no leaks survive filtering, and keeps provider-mode redaction, hashed audit logging, and the minimal prompt-budget guard intact. Added targeted core prompt tests plus CLI integration coverage for startup, focus handling, provider mode, and healthy-heap startup.
- **Step 11 large-dump scaling validation + analyzer fixes** — Enhanced `scripts/measure_rss.sh` to profile `parse`, `analyze`, and `leaks`, add `/proc/PID/status` VmHWM fallback sampling, compute RSS:dump ratios automatically, and mark results with pass/warn/fail indicators. Step 11 re-baselining found a 4.78x regression after unconditional `field_data` retention landed; remediation added public `ParseOptions` plus lean-by-default parser entry points so default `analyze`/`leaks` now measure ~656 MiB / 4.23x while `--threads --strings --collections` remains an opt-in ~741 MiB / 4.78x path on the 156 MB real-world fixture. The dense synthetic follow-through redesigned `SyntheticHeapApp.java` to produce high-cardinality, reference-heavy heaps, added generator density/growth tests, and validated ~500 MB / ~1 GB / ~2 GB tiers at 2.87x-2.90x on the default path and 3.89x-3.92x on the investigation path. The current in-memory architecture now clears the Step 11 gate.
- **M3 Phase 2 investigation features + Step 9 UX fix** — Added retained `field_data` plus selective primitive-array retention in `ObjectGraph`, parsed `STACK_TRACE` / `STACK_FRAME` records into stack-trace maps, added typed field readers (`FieldValue`, `read_field()`, `read_all_fields()`), and shipped four new analyzers: `inspect_threads()`, `analyze_strings()`, `inspect_collections()`, and `find_top_instances()`. `AnalyzeRequest` / `AnalyzeResponse` now carry optional analyzer enable flags and result sections; the CLI exposes `analyze --threads --strings --collections --top-instances --top-n --min-capacity`; and `mnemosyne leaks` now prints `No leak suspects detected.` on zero results.
- **M3-P1-B2 core analysis features** — Added `HistogramEntry`, `HistogramGroupBy`, `HistogramResult`, `UnreachableSet`, and `UnreachableClassEntry`; `analyze_heap()` now attaches grouped histogram and unreachable-object summaries; graph-backed leak ranking now uses retained/shallow ratio, accumulation-point detection, short reference-chain context, and composite suspect scores; `diff_heaps()` now adds class-level deltas; `AnalysisConfig` gained `accumulation_threshold`; and the CLI now supports `analyze --group-by` plus class-level diff output. Added 9 tests, bringing the workspace total to 110 passing tests.
- **M3-P1-B1 benchmark infrastructure + RSS measurement + HPROF tag centralization** — Added `core::hprof::tags` as the single source of truth for top-level record tags, heap-dump sub-record tags, and `tag_name()` across the parser, binary parser, fixtures, and GC-path code; added Criterion benches for parser throughput, graph construction, and dominator-tree work under `core/benches/`; added `scripts/measure_rss.sh` plus the `docs/design/memory-scaling.md` decision template; and fixed a GC-path edge-label regression uncovered during the refactor.
- **M1.5-B2 real-world validation + leak-ID hardening** — Added `validate_leak_id()` and wired it into CLI `explain`, `fix`, core `propose_fix`, and MCP `explain_leak` so unknown leak IDs now return `CoreError::InvalidInput` instead of silently falling back. Added 9 tests: 4 real-world HPROF integration tests for `parse`, `analyze`, `leaks`, and `gc-path`, 1 heuristic fallback validation test, 2 invalid leak-ID integration tests, and 2 unit tests for leak-ID validation. Real-world tests skip gracefully when `resources/test-fixtures/heap.hprof` is absent. The workspace now stands at 101 passing tests including 30 CLI integration cases.
- **M1.5-B1 tag fix + parser dispatch** — Corrected `TAG_HEAP_DUMP_SEGMENT` from `0x0D` to `0x1C` across the binary parser and GC-path code, fixed `tag_name()` mappings so `CPU_SAMPLES`, `CONTROL_SETTINGS`, `HEAP_DUMP_SEGMENT`, and `HEAP_DUMP_END` align with the HPROF spec, added `build_segment_fixture()` for `HEAP_DUMP_SEGMENT` coverage, and added 5 tests. This closes the real-world parser bug where JVM dumps using segmented heap records were previously skipped.
- **M2 CLI table output polish** — Added `comfy-table`-based aligned terminal tables for `mnemosyne parse` summary sections and `mnemosyne leaks`, kept formatting at the CLI boundary, corrected parse-summary wording to describe heap record-category aggregate bytes/entries instead of class-level retained-size semantics, and added truncation-safe disclosure sections so long leak IDs and class names remain fully visible. Added 4 CLI integration regressions for parse tables, leak tables, long-value disclosure, and colliding truncated leak IDs, bringing the workspace total to 87 tests including 23 CLI integration cases. This completes the Milestone 2 UX polish item for table-formatted summary output ahead of first-release follow-through.
- **M2-B6 better error messages** — Added structured `CoreError` variants for missing files, non-HPROF inputs, HPROF parse failures, and config errors; `validate_heap_file()` now suggests nearby `.hprof` files and flags common wrong extensions; the CLI top-level error handler prints colored `hint:` lines; and that batch added 3 new error-path integration tests. The workspace now stands at 87 passing tests.
- **M2-B4 Docker image** — Added a multi-stage `Dockerfile` and `.dockerignore` for a Rust builder plus `debian:bookworm-slim` runtime image, running `mnemosyne-cli` as a non-root user with `WORKDIR /data`, OCI labels, and `ENTRYPOINT ["mnemosyne-cli"]`. `.github/workflows/release.yml` now also builds and pushes `ghcr.io/<owner>/mnemosyne` on tagged releases with `<version>`, `<major>.<minor>`, and `latest` tags.
- **M2-B3 packaging** — Added crates.io-ready metadata across the workspace manifests, pinned `mnemosyne-core` versioned path dependencies in `cli/Cargo.toml` for publish/install compatibility, and added `HomebrewFormula/mnemosyne.rb` for Intel + Apple Silicon macOS release archives. v0.1.0 is now published on crates.io and the Homebrew SHA256 values are filled for the tagged release archives.
- **v0.1.1 documentation sync** — Updated owned documentation to reflect the grouped core module layout (`hprof/`, `graph/`, `analysis/`, `mapper/`, `report/`, `fix/`, `mcp/`) while preserving the public API re-export story in `core/src/lib.rs`.
- **M2-B2 release automation** — Added `.github/workflows/release.yml` to validate tag/version alignment, cross-compile `mnemosyne-cli` for five targets, package tar.gz/zip artifacts, and publish GitHub Releases with generated notes and attached binaries.
- **M1 foundations batch** — Added the canonical object-graph model now housed under `core::hprof::object_graph` so parser, dominator, retained-size, and GC-path work can converge on one shared graph model.
- **M1-B3 object-graph parser** — Added the binary HPROF parser now housed under `core::hprof::binary_parser` to read strings, classes, roots, instances, and arrays into the shared object graph.
- **M1-B4 dominator tree** — Added the dominator implementation now housed under `core::graph::dominator` with Lengauer-Tarjan immediate dominators, dominated-child lookup, and retained-size accumulation.
- **M1-B5 retained-size integration** — `analysis::analyze_heap()` now attempts graph-backed analysis first, emits real retained sizes and dominator metrics when available, and falls back with explicit provenance when filters or parsing prevent full graph-backed results.
- **M1-B6 graph-backed analysis completion** — `detect_leaks()` now follows the graph-backed path first, `gc_path` was rewritten with `ObjectGraph` BFS plus triple fallback, and `ObjectGraph` now exposes `get_object(id)`, `get_references(id)`, and `get_referrers(id)`.
- **M1-B7 integration coverage** — Added 16 CLI integration tests in `cli/tests/integration.rs` covering parse, leaks, analyze, gc-path, diff, fix, report, and config across synthetic HPROF fixtures.
- **Test-fixtures feature** — Added the `test-fixtures` cargo feature so integration tests can reuse canonical fixture builders such as `build_simple_fixture()` and `build_graph_fixture()` without widening the builder API surface.
- **Synthetic HPROF fixtures** — Added `core::test_fixtures` plus `resources/test-fixtures/README.md` so parser and graph code can exercise small deterministic heap shapes without committing large binaries.
- **CI workflow** — Added `.github/workflows/ci.yml` to run `cargo check`, `cargo test`, `cargo clippy -- -D warnings`, and `cargo fmt --check` on pushes and pull requests.
- **Provenance batch** — `ProvenanceKind` enum (`Synthetic`, `Partial`, `Fallback`, `Placeholder`) and `ProvenanceMarker` struct integrated into `AnalyzeResponse`, `LeakInsight`, `GcPathResult`, and `FixResponse`. Synthetic paths and fix suggestions are labeled automatically.
- **Output hardening** — `escape_html()` added to all user-controlled data in HTML reports (prevents XSS). `escape_toon_value()` added for TOON key-value output (handles backslashes, newlines, carriage returns).
- **Clippy cleanup** — Resolved warnings in the HPROF parsing and source-mapping modules during the v0.1.0 stabilization work.
- **Provenance rendering** — All non-JSON report formats (Text, Markdown, HTML, TOON) now render per-leak and response-level provenance markers. Three dedicated tests cover text, TOON, and HTML provenance output.
- **CLI provenance display** — `leaks`, `gc-path`, and `fix` CLI commands now print provenance markers when present.

When each must-have item above turns green, we can flip the README/architecture messaging from "roadmap" to "shipped" and retire the synthetic stand-ins currently in use.
